using Apex.AI.Components;
using System;

namespace Rust.Ai
{
	public class BaseAiUtilityClient : UtilityAIClient
	{
		public BaseAiUtilityClient(IntPtr pointer) : base(default(IntPtr))
		{
		}

	}
}
